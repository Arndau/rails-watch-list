<h1>Add Bookmarks to a list</h1>

ici, ajouter une liste dropdown de filmes,


<%= simple_form_for [@list, @bookmark] do |f| %>
  <%= f.association :movie %>
  <%= f.input :comment %>
  <%= f.submit "Submit", class: "btn btn-primary" %>
<% end %>
